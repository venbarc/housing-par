// Mock implementation – Firebase client is not used when running with mock data.
// The useCollection hook fetches from API routes instead of Firestore directly.

export const clientDb = null;
export const clientStorage = null;
